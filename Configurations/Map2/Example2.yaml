xStart:
 values: [-4, -17, 30]

xGoal:
  values: [-28, 28, 45]

nominalControl:
  values: [0, 0] # Nominal controls, v and omega

paramsLQR:
  values: [100, 1.0e-4, 100] # Horizont, epsilon, maximun iterations

quadraticFactors:
  values: [50, 1.0] # Q factor, R factor

obstaclesFactors:
  values: [1.0, 1.0, 1.675] # Obstacle factor, scale, robot radius

regressionFactors:
  startRadius: [0.5, 0.5, 0.3, 1, 1]
  minRadius: [0.01, 0.01, 0.01, 0.01, 0.01] 

completeRegressionFactors:
  startRadius: [0.9, 0.9, 0.3, 1, 1]
  minRadius: [0.01, 0.01, 0.01, 0.01, 0.01] 

mapFile:
  values: [map2.yaml]

     
  
