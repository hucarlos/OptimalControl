xStart:
 values: [0, -25, 180]

xGoal:
  values: [0, 25, 100]

nominalControl:
  values: [0.0, 0.0] # Nominal controls, v and omega

paramsLQR:
  values: [100, 1.0e-4, 100] # Horizont, epsilon, maximun iterations

quadraticFactors:
  values: [50, 1] # Q factor, R factor

obstaclesFactors:
  values: [1.0, 1.0, 1.676] # Obstacle factor, scale, robot radius

iQRLQR:
  epsilon: [1.0e-2] # epsilon
  radius: [1, 1, 1, 1, 1]

iQRSELQR:
  epsilon: [2.0e-3] # epsilon
  radius: [1, 1, 1, 1, 1] 

mapFile:
  values: [map1.yaml]

     
  
