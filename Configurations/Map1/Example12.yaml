xStart:
 values: [-21, 23, 15]

xGoal:
  values: [14, -17, 25]

nominalControl:
  values: [0.0, 0.0] # Nominal controls, v and omega

paramsLQR:
  values: [100, 1.0e-3, 100] # Horizont, epsilon, maximun iterations

quadraticFactors:
  values: [50, 1] # Q factor, R factor

obstaclesFactors:
  values: [2.0, 2.0, 1] # Obstacle factor, scale, robot radius

iQRLQR:
  epsilon: [0.001] # epsilon
  radius: [1, 1, 1, 1, 1]

iQRSELQR:
  epsilon: [0.001] # epsilon
  radius: [1, 1, 1, 1, 1] 

mapFile:
  values: [map1.yaml]

     
  
