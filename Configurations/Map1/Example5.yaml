xStart:
 values: [-12, 24, -45]

xGoal:
  values: [14, -19, -30]

nominalControl:
  values: [0.0, 0.0] # Nominal controls, v and omega

paramsLQR:
  values: [100, 1.0e-3, 100] # Horizont, epsilon, maximun iterations

quadraticFactors:
  values: [50, 1.0] # Q factor, R factor

obstaclesFactors:
  values: [1.0, 1.0, 1.675] # Obstacle factor, scale, robot radius

iQRLQR:
  epsilon: [0.01] # epsilon
  radius: [1, 1, 1, 1, 1]

iQRSELQR:
  epsilon: [1.0e-3] # epsilon
  radius: [1, 1, 1, 1, 1]

mapFile:
  values: [map1.yaml]



mapFile:
  values: [map1.yaml]

     
  
